{{#each locationList}}
<div>

    <h3>
        Address : {{address}}
    </h3>

    {{!-- <p>
        {{#if apartmentNumber}}
        Unit No:
        {{apartmentNumber}}
        {{/if}}
    </p>

    <p>{{city}}, {{state}} {{zip}}</p> --}}

    <a href="/locations/{{id}}">See Details</a>

    <hr>
</div>
{{/each}}